% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/us_counties.R
\docType{data}
\name{us_counties}
\alias{us_counties}
\title{U.S. counties and county-equivalents crosswalk}
\format{
A data frame with 3,143 rows (one per county/equivalent) and
the following columns:
\describe{
\item{fips}{5-digit FIPS county code as character (preserves leading zeros).}
\item{name}{County name in its full form, including the suffix
("County", "Parish", "Borough", "Census Area", "Municipality").}
\item{state}{Two-letter state abbreviation.}
}
}
\source{
Census Bureau FIPS codes master file.
}
\usage{
us_counties
}
\description{
Reference table of U.S. counties, parishes, boroughs, census areas, and
municipalities, derived from the Census Bureau FIPS master file. Used
internally by \code{\link[=validate_general_model]{validate_general_model()}} to validate \code{location} values
when \code{location_general = "county"}.
}
\details{
County names retain their full form (e.g., \code{"Autauga County"},
\code{"Aleutians East Borough"}, \code{"Orleans Parish"}). The validator matches
user-supplied county names against a normalized form (lowercase, with
the suffix stripped), so users may enter either \code{"Aiken"} or
\code{"Aiken County"}.
}
\keyword{datasets}
