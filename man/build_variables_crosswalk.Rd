% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_variables_crosswalk.R
\name{build_variables_crosswalk}
\alias{build_variables_crosswalk}
\title{Build a variables crosswalk file}
\usage{
build_variables_crosswalk(
  config,
  implementation_model = NULL,
  evaluation_model = NULL,
  force = FALSE
)
}
\arguments{
\item{config}{A validated configuration list produced by
\code{validate_report_params()}.}

\item{implementation_model}{A validated implementation model data frame
produced by the appropriate hub validator (e.g.,
\code{validate_flusight_model()}), or \code{NULL} if not available.}

\item{evaluation_model}{A validated evaluation model data frame produced
by \code{validate_eval_model()}, or \code{NULL} if not available.}

\item{force}{Logical. If \code{TRUE} and the output file already exists, the
file is overwritten after a loud warning. If \code{FALSE} (default) and the
file already exists, the function stops with an error and requires the
user to re-run with \code{force = TRUE} to confirm the overwrite.}
}
\value{
Invisibly returns the path to the written file.
}
\description{
Seeds a variables crosswalk CSV from the user's validated report
configuration and loaded model files. The seeded file contains standard
glossary rows, one data source row per location/disease group, and one
outcome row per group. Users complete the file by filling in report-facing
names, definitions, and metadata, then pass it to
\code{validate_variables_crosswalk()} as part of report generation.
}
\details{
The output file is written to the current working directory. The filename
is constructed from the contact name, disease, reason, and model type
stored in \code{config}.
}
