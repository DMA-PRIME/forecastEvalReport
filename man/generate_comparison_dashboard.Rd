% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_comparison_dashboard.R
\name{generate_comparison_dashboard}
\alias{generate_comparison_dashboard}
\title{Generate an interactive forecast model comparison dashboard}
\usage{
generate_comparison_dashboard(
  options_files,
  output_file = "forecast-model-comparison.html",
  title = "Model Summaries",
  evaluation = c("both", "testing", "realtime"),
  eval_config = NULL,
  include_row_metrics = TRUE,
  overwrite = TRUE,
  quiet = TRUE
)
}
\arguments{
\item{options_files}{Character vector containing paths to two or more completed report options \code{.R} files.}

\item{output_file}{Path for the resulting \code{.html} dashboard. Defaults to \code{"forecast-model-comparison.html"} in the current working directory.}

\item{title}{Character title shown at the top of the dashboard.}

\item{evaluation}{Character. Which evaluation results to include: \code{"testing"}, \code{"realtime"}, or \code{"both"} (default). Testing requires an evaluation model with testing-period rows. Real-time requires an implementation model and scores the rolling \code{Forecasts/} archive against observations available through the options file.}

\item{eval_config}{Optional named list from \code{create_evaluation_config()}. When \code{NULL} (default), the package defaults are used for every model.}

\item{include_row_metrics}{Logical. When \code{TRUE} (default), the dashboard data includes both summary and per-forecast metrics. Set to \code{FALSE} for a smaller HTML file containing only overall and horizon summaries.}

\item{overwrite}{Logical. When \code{TRUE} (default), an existing output file is replaced. When \code{FALSE}, the function stops before doing any model work.}

\item{quiet}{Logical. When \code{TRUE} (default), suppresses pipeline progress messages. Validation errors and the final dashboard path are still shown.}
}
\value{
Invisibly returns the normalized path to the generated HTML file.
}
\description{
Validates and processes two or more completed report options files through the same data-preparation and evaluation pathways used by the package report, then writes one self-contained HTML dashboard. The dashboard compares current, testing-period, and real-time archived median forecasts, observed values, all available performance metrics, data coverage, and a filterable table of model options, evaluation settings, crosswalk terms, and plain-language definitions. Report-facing disease, outcome, model, and location labels are cleaned before display. It remains an HTML file and requires no web server or internet connection after it is created.
}
\details{
Every options file must reference a completed variables crosswalk and observed outcome data. Testing evaluation requires an evaluation model containing a testing period. Real-time evaluation requires an implementation model and uses the rolling forecast archive maintained by the package report. Validation is all-or-nothing: if any options file fails the checks needed by the selected mode, no dashboard is written and the combined error identifies each file that needs attention.
}
\examples{
\dontrun{
generate_comparison_dashboard(
  options_files = c(
    "models/arima/report_options.R",
    "models/ets/report_options.R"
  ),
  output_file = "model-comparison.html"
)
}
}
