% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_evaluation_table.R
\name{build_evaluation_table}
\alias{build_evaluation_table}
\title{Build a wide comparison table from testing-evaluation results}
\usage{
build_evaluation_table(
  x,
  family = NULL,
  scope = NULL,
  metric = NULL,
  columns = "model",
  output_file = NULL,
  quiet = TRUE
)
}
\arguments{
\item{x}{Either a character vector of CSV paths / a directory (passed to
\code{read_testing_evaluations()}), or a long-format data frame already in the
export schema.}

\item{family}{Optional character. Keep only this metric family
(\code{percentAgreement}, \code{forecastBias}, \code{peakPhase}, \code{traditional}).}

\item{scope}{Optional character. Keep only this scope (\code{overall} or
\code{horizon}).}

\item{metric}{Optional character vector of metric names to keep.}

\item{columns}{Character naming the column dimension of the wide table.
Either \code{"model"} (default) or \code{"source_file"}.}

\item{output_file}{Optional path. When supplied, the wide table is written
there as a CSV.}

\item{quiet}{Logical. When \code{TRUE} (default), suppresses progress messages.}
}
\value{
Invisibly, a named list with \code{long} (the filtered long frame) and
\code{table} (the wide comparison data frame).
}
\description{
Takes the long-format results produced by
\code{export_testing_evaluation(save_results = TRUE)} -- either as CSV files or
as an already-stacked data frame from \code{read_testing_evaluations()} -- and
reshapes them into a wide comparison table. By default each row is one
(family, scope, location, season, horizon, metric) combination and each
column is a model, so models sit side by side for easy comparison. Peak
rows carry the season they belong to; other families leave it \code{NA}. The
column dimension
can be switched to \code{source_file} when comparing repeated runs of the same
model. Optional filters narrow the table to a single metric family, scope,
or metric name before pivoting.
}
\details{
The cell shown is the numeric \code{value} when present, otherwise the text
\code{value_chr} label, so numeric metrics and labelled metrics both render in
one table.
}
