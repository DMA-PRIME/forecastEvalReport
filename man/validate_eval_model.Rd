% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_eval_model.R
\name{validate_eval_model}
\alias{validate_eval_model}
\title{Validate an evaluation model submission file}
\usage{
validate_eval_model(file, verbose = FALSE, state_context = NA_character_)
}
\arguments{
\item{file}{Path to an evaluation model submission CSV file.}

\item{verbose}{Logical. If \code{TRUE}, prints a summary of the validated
file on success. Defaults to \code{FALSE}.}

\item{state_context}{Optional two-letter U.S. state abbreviation (or
\code{NA}). When provided, county and HSA validation is restricted to
that state. Typically supplied via \code{config$state_context} from
\code{validate_report_params()}.}
}
\value{
A data frame containing the loaded evaluation forecast data,
with \code{location} preserved as character.
}
\description{
Performs a full validation of an evaluation-period forecast submission
file against the general-format specification, with evaluation-specific
adjustments. The evaluation file represents forecasts produced during
the training/validation/testing period and is used for plotting,
evaluation, and comparison with the operational implementation forecast.
}
\details{
Differences from \code{\link[=validate_general_model]{validate_general_model()}}: \code{training_validation} may
be \code{NA} in addition to \code{0/1/2}; \code{horizon} may be any
integer (not restricted to \code{0/1/2}); \code{output_type_id} must be exactly
\code{0.5} (point forecasts only); each forecast group must contain exactly
one row; and all rows must have \code{target_end_date < reference_date}
(i.e., the file must contain only historical evaluation rows; users are
responsible for filtering future rows themselves).
}
