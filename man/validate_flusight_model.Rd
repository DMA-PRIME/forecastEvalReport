% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_flusight_model.R
\name{validate_flusight_model}
\alias{validate_flusight_model}
\title{Validate a FluSight forecast submission file}
\usage{
validate_flusight_model(file, verbose = FALSE)
}
\arguments{
\item{file}{Path to a FluSight forecast submission CSV file.}

\item{verbose}{Logical. If \code{TRUE}, prints a summary of the validated
file on success. Defaults to \code{FALSE}.}
}
\value{
A data frame containing the loaded forecast data, with
\code{location} preserved as character to keep leading zeros in FIPS codes.
}
\description{
Performs a full validation of a forecast submission file against the
FluSight 2025-2026 hubverse-format specification. Checks file structure,
column types, allowed values, cross-column logical consistency, and
group-level completeness rules. Errors are collected within each phase
and reported together; a phase that produces errors halts subsequent
phases that depend on it.
}
\details{
Modeling-correctness rules (such as whether a \code{pmf} rate-change
category was correctly assigned given the underlying incidence forecast)
are out of scope: this function validates file format, not modeling
decisions.
}
