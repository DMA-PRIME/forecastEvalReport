% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_report_pdf.R
\name{save_report_pdf}
\alias{save_report_pdf}
\title{Save a printable PDF copy of the forecast evaluation report}
\usage{
save_report_pdf(
  options_file = NULL,
  html_file = NULL,
  output_dir = getwd(),
  output_file = NULL,
  paper = c("tabloid-landscape", "tabloid", "letter-landscape", "letter",
    "legal-landscape", "legal", "a3-landscape", "a3", "a4-landscape", "a4", "custom"),
  width = NULL,
  height = NULL,
  margin = 0.4,
  scale = 1,
  wait = 8,
  background = TRUE,
  expand_accordions = TRUE,
  expand_legends = TRUE,
  hide_controls = TRUE,
  keep_html = FALSE,
  overwrite = TRUE,
  quiet = TRUE,
  ...
)
}
\arguments{
\item{options_file}{Path to a completed report options \code{.R} file produced by
\code{create_options_template()} -- the same file \code{generate_report()} uses. The
report is rendered to HTML in a temporary directory, then printed. Leave
\code{NULL} when supplying \code{html_file}.}

\item{html_file}{Path to an already-rendered report \code{.html} file (or a vector
of them, as produced by \code{generate_report(split_by_location = TRUE)}). One
PDF is written per file. Leave \code{NULL} when supplying \code{options_file}.}

\item{output_dir}{Directory where the PDF should be saved. Defaults to the
current working directory.}

\item{output_file}{Name of the PDF, ending in \code{.pdf}. When \code{NULL} (default)
the name of the source HTML report is reused with a \code{.pdf} extension.
Ignored when several \code{html_file} paths are supplied, since each keeps its
own name.}

\item{paper}{Page size. One of \code{"tabloid-landscape"} (default), \code{"tabloid"},
\code{"letter-landscape"}, \code{"letter"}, \code{"legal-landscape"}, \code{"legal"},
\code{"a3-landscape"}, \code{"a3"}, \code{"a4-landscape"}, \code{"a4"}, or \code{"custom"}. Use
\code{"custom"} with \code{width} and \code{height} for any other size.}

\item{width, height}{Page size in inches. Used only when \code{paper = "custom"}.}

\item{margin}{Page margin in inches, applied to all four sides. A single
non-negative number; defaults to \code{0.4}.}

\item{scale}{Zoom applied to the page content, between \code{0.1} and \code{2}.
Defaults to \code{1} (the report at its true size). Lower values fit more
content across a narrower page.}

\item{wait}{Seconds to wait after the page loads before printing, so the
Plotly figures can finish drawing. Defaults to \code{8}. Raise it if figures
come out blank or half-drawn.}

\item{background}{Logical. Print background colors and images? \code{TRUE}
(default) keeps striped tables, phase ribbons, and accordion shading, so
the PDF matches the screen. \code{FALSE} gives a lighter, ink-saving page.}

\item{expand_accordions}{Logical. Open every collapsed \verb{<details>} accordion
before printing so its contents appear in the PDF? Defaults to \code{TRUE}.
\code{FALSE} prints only the headers of any section left collapsed.}

\item{expand_legends}{Logical. Expand each plot's floating legend and place
it beside the figure instead of over it? Defaults to \code{TRUE}, matching the
static exports from \code{save_forecast_plot()}.}

\item{hide_controls}{Logical. Hide interactive-only controls -- the Plotly
modebar, the fullscreen button, and the geography dropdown -- that have no
meaning on paper? Defaults to \code{TRUE}.}

\item{keep_html}{Logical. Keep the intermediate print-ready HTML next to the
PDF? Defaults to \code{FALSE}. Useful for inspecting what was sent to Chrome
when a PDF does not come out as expected.}

\item{overwrite}{Logical. Overwrite an existing PDF? Defaults to \code{TRUE}.}

\item{quiet}{Logical. If \code{TRUE} (default), suppresses rendering and printing
console output. Set to \code{FALSE} for verbose output useful when debugging.}

\item{...}{Further arguments passed to \code{generate_report()} when rendering
from \code{options_file} (for example \code{plot_styles}, \code{eval_config},
\code{location_crosswalk}, or \code{population_crosswalk}).}
}
\value{
Invisibly returns the path(s) to the saved PDF file(s).
}
\description{
Produces a paginated PDF of the forecast evaluation report that matches the
interactive HTML version. The PDF is printed from the rendered report itself
using headless Chrome, so the fonts, colors, table styling, and Plotly
figures are the ones the report already shows -- nothing is re-drawn or
re-styled for print.
}
\details{
Supply either an \code{options_file} (the same completed options file
\code{generate_report()} takes, which is rendered to HTML first) or an
\code{html_file} that has already been rendered. Supplying both is an error.

Three things are adjusted before printing, because they exist only to
support interaction and would otherwise leave the PDF with content missing:
collapsed \verb{<details>} accordions are opened so their contents appear, the
floating plot legends are expanded and pinned beside the figure rather than
floating over it, and controls that cannot be used on paper -- the Plotly
modebar, the fullscreen button, and the geography dropdown -- are hidden.
Every one of these can be turned off individually. Whichever geography the
report opens on is the one that prints, matching the on-screen view.

The report body is 1500px wide, so the default page is tabloid landscape
(17 x 11in), which fits that width at 96dpi without shrinking the layout.
Narrower paper still works -- pass \code{scale} below \code{1} to fit more across --
but the layout will be reduced accordingly.

Printing requires \pkg{pagedown} (which drives headless Chrome through
\pkg{chromote}). Chrome or Chromium must be installed and discoverable; see
\code{pagedown::find_chrome()} if it is not found automatically.
}
\examples{
\dontrun{
  # From a completed options file, straight to a PDF
  save_report_pdf("report_options.R", output_dir = "exports")

  # From a report that has already been rendered
  save_report_pdf(html_file = "exports/Smith-Influenza-Testing-ARIMA.html")

  # Letter landscape, scaled down to fit the 1500px body
  save_report_pdf(
    "report_options.R",
    paper = "letter-landscape",
    scale = 0.62
  )

  # Every per-location report from a split run
  paths <- generate_report("report_options.R", split_by_location = TRUE)
  save_report_pdf(html_file = paths, output_dir = "exports/pdf")
}
}
