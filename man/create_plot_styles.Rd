% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_plot_styles.R
\name{create_plot_styles}
\alias{create_plot_styles}
\title{Create a plot styles configuration for forecast evaluation reports}
\usage{
create_plot_styles(
  colors = c("#898989"),
  pi_intervals = list(`50\%` = list(lower = 0.25, upper = 0.75, fill =
    "rgba(100,143,255,0.15)", hover_color = "#648FFF", label = "50\% PI", swatch_class =
    "pi-50"), `95\%` = list(lower = 0.025, upper = 0.975, fill =
    "rgba(100,143,255,0.08)", hover_color = "#648FFF", label = "95\% PI", swatch_class =
    "pi-95")),
  horizon_styles = list(`1` = list(color = "#648FFF", dash = "solid", width = 1.5), `2` =
    list(color = "#FE6100", dash = "dash", width = 1.5), `3` = list(color = "#DC267F",
    dash = "dot", width = 1.5), `4` = list(color = "#785EF0", dash = "dashdot", width =
    1.5)),
  target_data = list(color = "#000000", dash = "solid", width = 2, marker_size = 4),
  current_projections = list(color = "#648FFF", dash = "solid", width = 2, marker_size =
    4),
  historical_estimates = list(color = "#648FFF", dash = "dot", width = 1.5, marker_size =
    3),
  phase_colors = list(`Training Period` = "#345FAF", `Validation Period` = "#E4572E",
    `Testing Period` = "#2E8B57"),
  plot_width = 1000,
  plot_height = 800,
  default_view_weeks = 52L,
  rangeslider_bgcolor = "#ffffff",
  eval_legend_title = "Evaluation Model",
  right_axis = list(title_percent = "Auxiliary Variables", title_default =
    "Weekly Tests", percent_outcomes = c("Weekly \% Flu-Attributable ED-Visits",
    "Weekly \% RSV-Attributable ED-Visits", "Weekly \% COVID-19-Attributable ED-Visits"),
    title_font_size = 16, title_font_color = "#111", title_standoff = 25)
)
}
\arguments{
\item{colors}{Character vector of hex color codes used for the primary
color palette (implementation model, general traces). Defaults to a
blue-purple colorblind-friendly palette.}

\item{pi_intervals}{Named list defining prediction interval levels.
Each entry must have: \code{lower} (lower quantile), \code{upper} (upper
quantile), \code{fill} (rgba fill color string), \code{hover_color} (hex
color for hover), \code{label} (display label), \code{swatch_class} (CSS
class for the legend swatch).}

\item{horizon_styles}{Named list defining per-horizon line styles for
the evaluation model. Each entry must have: \code{color}, \code{dash}, \code{width}.
Names should match the horizon values in the evaluation model file
(e.g., \code{"1"}, \code{"2"}, \code{"3"}, \code{"4"}).}

\item{target_data}{Named list with style settings for the target data
(truth) trace. Must have: \code{color}, \code{dash}, \code{width}, \code{marker_size}.}

\item{current_projections}{Named list with style settings for the
current projections trace. Must have: \code{color}, \code{dash}, \code{width},
\code{marker_size}.}

\item{historical_estimates}{Named list with style settings for the
historical estimates trace. Must have: \code{color}, \code{dash}, \code{width},
\code{marker_size}.}

\item{phase_colors}{Named list of hex color codes for the modeling
period phase ribbons. Must have entries named
\code{"Training Period"}, \code{"Validation Period"}, \code{"Testing Period"}.}

\item{plot_width}{Numeric. Plot width in pixels. Default \code{900}.}

\item{plot_height}{Numeric. Plot height in pixels. Default \code{500}.}

\item{default_view_weeks}{Integer. Number of weeks shown in the
default range slider window. Default \code{52}.}

\item{rangeslider_bgcolor}{Character. Background color of the range
slider. Default \code{"#ffffff"}.}

\item{eval_legend_title}{Character. Legend group title for the
evaluation model section. Default \code{"Evaluation Model"}.}

\item{right_axis}{Named list configuring the right y-axis used for
auxiliary variable traces. Must have: \code{title_percent}, \code{title_default},
\code{percent_outcomes}, \code{title_font_size}, \code{title_font_color},
\code{title_standoff}.}
}
\value{
A named list of plot style settings for use by the report
plotting functions.
}
\description{
Builds a named list of visual style settings used throughout the
forecast evaluation report plots. All arguments have sensible defaults
that match the standard report appearance. Users who want to customize
the report appearance create their own styles object by calling this
function with overrides, then source it in their options file and pass
it to \code{\link[=generate_report]{generate_report()}}.
}
