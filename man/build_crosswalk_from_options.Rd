% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_crosswalk_from_options.R
\name{build_crosswalk_from_options}
\alias{build_crosswalk_from_options}
\title{Build a variables crosswalk from a completed options file}
\usage{
build_crosswalk_from_options(options_file, force = FALSE)
}
\arguments{
\item{options_file}{Path to a completed report options \code{.R} file
produced by \code{\link[=create_options_template]{create_options_template()}}. The
\code{variables.crosswalk.file} field does not need to be filled in at
this stage.}

\item{force}{Logical. Passed to \code{\link[=build_variables_crosswalk]{build_variables_crosswalk()}}. If
\code{TRUE}, overwrites an existing crosswalk file. Defaults to \code{FALSE}.}
}
\value{
Invisibly returns the path to the written crosswalk CSV.
}
\description{
Reads a completed report options file, validates all parameters, loads
and validates the model files, and calls \code{build_variables_crosswalk()}
to seed the variables crosswalk CSV. This is the intended entry point
for crosswalk generation and should be run after filling in the options
file but before completing the crosswalk and running \code{generate_report()}.
}
