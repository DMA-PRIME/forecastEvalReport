% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_testing_evaluations.R
\name{read_testing_evaluations}
\alias{read_testing_evaluations}
\title{Read and stack testing-evaluation result CSVs}
\usage{
read_testing_evaluations(files, quiet = TRUE)
}
\arguments{
\item{files}{Either a character vector of CSV file paths, or a single
directory path, in which case every \code{.csv} file in that directory is
read.}

\item{quiet}{Logical. When \code{TRUE} (default), suppresses the per-file
progress messages.}
}
\value{
A single long-format data frame with the export schema plus a
\code{source_file} column.
}
\description{
Reads one or more long-format CSV files written by
\code{export_testing_evaluation(save_results = TRUE)} and stacks them into a
single tidy data frame. Each input file must carry the fixed export schema
(\code{model}, \code{location}, \code{horizon}, \code{scope}, \code{family}, \code{metric}, \code{value},
\code{value_chr}). An optional \code{season} column is carried through when present
and filled with \code{NA} for older files that predate it; a \code{source_file}
column is added so rows can be traced back to the file they came from.
This is the input layer for
\code{build_evaluation_table()}, but the stacked frame is useful on its own for
custom summaries across models or runs.
}
