% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_realtime_evaluation.R
\name{export_realtime_evaluation}
\alias{export_realtime_evaluation}
\title{Export real-time evaluation as AI-ready Markdown, JSON, and CSV}
\usage{
export_realtime_evaluation(
  options_file,
  output_dir = getwd(),
  ai_form = FALSE,
  save_results = FALSE,
  ai_row_detail = FALSE,
  eval_config = NULL,
  file_prefix = NULL,
  quiet = TRUE,
  return_context = FALSE,
  save_json = FALSE,
  archive_current = TRUE,
  population_crosswalk = NULL
)
}
\arguments{
\item{options_file}{Completed report options R file with an implementation
model and variables crosswalk. An evaluation model is optional.}

\item{output_dir}{Output directory, created when necessary.}

\item{ai_form}{Write an AI-ready Markdown summary. Default FALSE.}

\item{save_results}{Write the complete long-format results CSV. Default FALSE.}

\item{ai_row_detail}{Include per-forecast tables in Markdown. JSON and CSV
always include per-forecast results. Default FALSE.}

\item{eval_config}{Settings from create_evaluation_config(), or NULL for defaults.}

\item{file_prefix}{Optional filename stem, without directory separators.}

\item{quiet}{Suppress preparation progress messages. Default TRUE.}

\item{return_context}{Include validated input context in the returned R object.}

\item{save_json}{Write standalone JSON with metadata, settings, results,
forecast/truth pairs, metric definitions, WIS diagnostics, and trend calibration.}

\item{archive_current}{Archive the current implementation forecast first.
Default TRUE. FALSE evaluates the existing archive without adding a snapshot.}

\item{population_crosswalk}{Optional population lookup for trend labels,
as a named numeric vector or location/population data frame.}
}
\value{
Invisibly, a list with realtime_eval, results, payload, paths, and
optional context. Unrequested file paths are NA. Empty evaluations are
explicitly marked no_observed_targets, not reported as zero error.
}
\description{
Scores archived operational forecasts against the selected observed truth,
using the report's real-time preparation pathway. All realized weeks are
evaluated; testing-only peak metrics are omitted. By default the current
implementation forecast is archived first, as when generating a report.
This produces data for AI interpretation, not an AI-generated narrative.
No external AI service is called and no HTML report is rendered.
}
