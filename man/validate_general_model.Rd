% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_general_model.R
\name{validate_general_model}
\alias{validate_general_model}
\title{Validate a general-format implementation model submission file}
\usage{
validate_general_model(file, verbose = FALSE, state_context = NA_character_)
}
\arguments{
\item{file}{Path to a general-format submission CSV file.}

\item{verbose}{Logical. If \code{TRUE}, prints a summary of the validated
file on success. Defaults to \code{FALSE}.}

\item{state_context}{Optional two-letter U.S. state abbreviation (or
\code{NA}). When provided, county and HSA validation is restricted to
that state. Typically supplied via \code{config$state_context} from
\code{validate_report_params()}.}
}
\value{
A data frame containing the loaded forecast data, with
\code{location} preserved as character.
}
\description{
Performs a full validation of a forecast submission file against the
general-format specification used internally for Software and Internal
reasons. Supports multiple \code{location_general} geographies (state,
national, county, HSA, ZCTA, ZIP, region, and custom). Cross-walks are
applied per geography type, with optional \code{state_context} to disambiguate
county and HSA matches.
}
\details{
For HSA validation, the validator first checks the location against
\link{metrocast_locations}, then falls back to \link{dartmouth_hsa_zip} if no
match is found. ZIP and ZCTA are checked for length and digit format
only; no crosswalk membership is enforced.
}
