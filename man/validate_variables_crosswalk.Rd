% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_variables_crosswalk.R
\name{validate_variables_crosswalk}
\alias{validate_variables_crosswalk}
\title{Validate a variables crosswalk file}
\usage{
validate_variables_crosswalk(x, verbose = FALSE)
}
\arguments{
\item{x}{Either a path to a crosswalk CSV file (character), or an
already-loaded crosswalk data frame. Passing a data frame is intended
for in-report validation; passing a path is intended for standalone
checking outside report generation.}

\item{verbose}{Logical. If \code{TRUE}, prints a summary of the validated
crosswalk on success. Defaults to \code{FALSE}.}
}
\value{
A data frame containing the validated crosswalk, with the
\code{on_right_axis} and \code{convert_percent} columns normalized to logical
values.
}
\description{
Performs a full validation of a variables crosswalk against the
forecastEvalReport crosswalk specification. The crosswalk maps variables
(data sources, auxiliary variables, outcomes, and general glossary terms)
to report-facing names, definitions, and metadata. Errors are collected
within each phase and reported together; a phase that produces errors
halts subsequent phases that depend on it.
}
\details{
The crosswalk is typically produced by \code{build_variables_crosswalk()},
completed by the user, and validated here as part of report generation.
}
