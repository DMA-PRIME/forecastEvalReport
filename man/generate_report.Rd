% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_report.R
\name{generate_report}
\alias{generate_report}
\title{Generate a forecast evaluation report}
\usage{
generate_report(
  options_file,
  output_dir = getwd(),
  output_file = NULL,
  quiet = TRUE,
  plot_styles = NULL,
  eval_config = NULL,
  plot_export = NULL,
  split_by_location = FALSE,
  split_filename = NULL,
  location_crosswalk = NULL
)
}
\arguments{
\item{options_file}{Path to a completed report options \code{.R} file
produced by \code{create_options_template()}.}

\item{output_dir}{Directory where the rendered HTML report should be
saved. Defaults to the current working directory.}

\item{output_file}{Name of report. It must end with .html. When
\code{split_by_location = TRUE}, the location name is appended to this base
name for each per-location file (e.g. \code{myreport-Upstate.html}).}

\item{quiet}{Logical. If \code{TRUE} (default), suppresses
\code{rmarkdown::render()} console output. Set to \code{FALSE} for verbose
rendering output useful during debugging.}

\item{plot_styles}{A named list of plot style settings produced by
\code{create_plot_styles()}. If \code{NULL} (default), the report uses the
default styles returned by \code{create_plot_styles()} with no arguments.}

\item{eval_config}{Optional named list of evaluation tuning settings
produced by \code{create_evaluation_config()}. If \code{NULL} (default), package
defaults are used.}

\item{plot_export}{Optional named list of forecast-plot export settings,
normally supplied via \code{save_forecast_plot()} rather than directly. When
non-\code{NULL}, the report is not rendered in full: the forecast-plot section
is built and written to the requested file(s), the rest of the report is
skipped, and the saved path(s) are returned instead of an HTML report.
When \code{NULL} (default), a normal report is generated. Ignored (with a
warning) when combined with \code{split_by_location = TRUE}.}

\item{split_by_location}{Logical. If \code{FALSE} (default), a single report is
produced with all locations behind the in-report geography dropdown. If
\code{TRUE}, one self-contained report is produced per location: the model
file(s) are filtered to each location and rendered separately, and the
vector of all saved report paths is returned. The set of locations is
taken from the \code{location} column of the implementation model file (or the
evaluation model file when no implementation file is supplied), matching
the location universe of the combined report.}

\item{split_filename}{Optional character template controlling where each
per-location report is written when \code{split_by_location = TRUE}. It is
interpreted relative to \code{output_dir} and must contain at least one
location placeholder so every report gets a distinct path:
\code{{location}} is a filesystem-safe label (e.g. \code{"Pee_Dee"}) and
\code{{location_name}} is the raw location value (e.g. \code{"Pee Dee"}). The
template may include subdirectories (created as needed) and must end in
\code{.html}. When \code{NULL} (default), the location is appended to the base
filename as \verb{-<Location>}. Ignored (with a warning) when
\code{split_by_location = FALSE}. Examples:
\code{"region-covid_19-inpatient-{location}.html"} writes flat files with the
location at the end; \code{"{location}/report.html"} writes one subfolder per
location.}

\item{location_crosswalk}{Optional user-supplied mapping from raw location
values to cleaned display names. Raw codes are kept for archived forecast
filenames and the forecast data; the cleaned names are shown throughout
the report (and used in the assembled master data set). May be a path to a
two-column CSV with columns \code{location} (the raw value, exactly as it
appears in your model files' \code{location} column) and \code{clean_name} (the
display name), a \code{data.frame} with those columns, or \code{NULL} (default) to
use the package's built-in location lookups. Matching is exact after
trimming whitespace; only listed locations are overridden, and a supplied
name takes precedence over the built-in tables.}
}
\value{
Invisibly returns the path to the rendered HTML report; a character
vector of paths (one per location) when \code{split_by_location = TRUE}; or --
when \code{plot_export} is supplied -- the path(s) to the saved plot file(s).
}
\description{
Reads a completed report options file, validates all parameters, and
renders the forecast evaluation RMarkdown report to an HTML file. This
is the primary user-facing entry point for report generation.
}
\details{
The options file is an R script that defines a list named
\code{report_options}. Create a template using \code{create_options_template()},
fill in all fields, build and complete the variables crosswalk using
\code{build_crosswalk_from_options()}, add the crosswalk path to the options
file, then call this function.

Plot appearance can be fully customized by passing a styles object
produced by \code{create_plot_styles()}. If no styles object is provided,
the default appearance is used automatically.

By default a single report is produced containing every location in the
model file(s), switchable via the in-report geography dropdown. Set
\code{split_by_location = TRUE} to instead produce one self-contained report
per location: the implementation and evaluation model files are filtered
to each location in turn and rendered individually. This automates the
otherwise-manual workflow of pre-filtering the model CSVs by location and
looping over a set of per-location options files.
}
