% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_forecast_plot.R
\name{save_forecast_plot}
\alias{save_forecast_plot}
\title{Save the forecast plot without rendering the full report}
\usage{
save_forecast_plot(
  options_file,
  file = NULL,
  format = c("html", "png", "jpeg", "jpg", "pdf", "tiff", "tif"),
  plot = c("forecast", "consistency"),
  location = NULL,
  width = NULL,
  height = NULL,
  scale = 1,
  delay = NULL,
  n_forecasts = 5L,
  forecast_every = NULL,
  end_cushion = NULL,
  font_size = NULL,
  tick_size = NULL,
  forecast_size = NULL,
  forecast_color = NULL,
  observed_size = NULL,
  observed_color = NULL,
  aux_size = NULL,
  aux_color = NULL,
  title_gap = NULL,
  view_start = NULL,
  view_end = NULL,
  legend = TRUE,
  overwrite = TRUE,
  plot_styles = NULL,
  eval_config = NULL,
  quiet = TRUE
)
}
\arguments{
\item{options_file}{Path to a completed report options \code{.R} file produced by
\code{create_options_template()} -- the same file \code{generate_report()} uses.}

\item{file}{Where to save the plot: a full file path (e.g.
\code{"exports/upstate.pdf"}) or a directory. When a directory is given, or when
every geography is saved, files are auto-named \verb{forecast_plot_<location>}.
Defaults to the current working directory.}

\item{format}{Output format: one of \code{"html"}, \code{"png"}, \code{"jpeg"} (alias
\code{"jpg"}), \code{"pdf"}, or \code{"tiff"} (alias \code{"tif"}). If omitted, inferred from
the \code{file} extension when possible, otherwise \code{"html"}. TIFF is written
lossless at 300 dpi (needs the \code{magick} package).}

\item{plot}{Which figure to export: \code{"forecast"} (default) for the main
forecast plot, or \code{"consistency"} for the forecast-consistency plot.}

\item{location}{Which geography to export: a display name (e.g. \code{"Upstate"})
or a positive integer index into the alphabetically-ordered geographies.
\code{NULL} (default) exports every geography. For static formats, multiple
geographies are written one file each into the directory given by \code{file};
for HTML, a single geography is rendered as its own page, and \code{NULL}
produces the usual all-geographies page with the dropdown.}

\item{width, height}{Viewport size in pixels for a static capture. \code{NULL}
(default) uses 1200 x 750.}

\item{scale}{Resolution multiplier for a static capture (passed to
\pkg{webshot2} as \code{zoom}). Higher values give sharper images. Defaults
to \code{1}.}

\item{delay}{Seconds \pkg{webshot2} waits before capturing a static image,
so the plot can finish drawing and the page can expand to the side-legend
view. \code{NULL} (default) picks the value automatically: a longer wait for
\code{plot = "consistency"} (its many overlaid forecasts take longer to render)
and the standard wait for the main forecast plot. Raise it if a static
export comes out blank.}

\item{n_forecasts}{Integer. For \code{plot = "consistency"} only: how many of the
most-recent forecasts to load and show in the export. A static image
cannot toggle forecasts on, so this is exactly what appears. Defaults to
\code{5}.}

\item{forecast_every}{Optional positive integer. For \code{plot = "consistency"}
only: instead of the \code{n_forecasts} most-recent forecasts shown
consecutively, overlay every Xth forecast across the view window, starting
at the beginning of that window (\code{view_start} when supplied, otherwise the
earliest forecast). Every qualifying forecast on the stride is shown -- the
view window bounds how many appear, and \code{n_forecasts} does not cap stride
mode. \code{NULL} (default) keeps the consecutive most-recent behavior. For
example, with a Jan--Apr window, \code{forecast_every = 4} shows forecasts 1, 5,
9, ... from the window start through its end.}

\item{end_cushion}{Optional positive integer used only with \code{forecast_every}
(stride mode). Drops the most-recent N forecasts from the window before
striding, so the stride stops short of the very last forecast(s) -- useful
when you don't want the latest forecast included. \code{NULL} (default) keeps
them.}

\item{font_size}{Optional single positive number giving the base font size
(points) for the whole figure -- axis titles, tick labels, the legend, and
hover. \code{NULL} (default) keeps the configured plot styling.}

\item{tick_size}{Optional single positive number setting the axis tick-label
(the axis numbers/dates) size in points, independent of \code{font_size} -- use
it to enlarge the tick labels without changing the axis titles. For
\code{plot = "consistency"} only. \code{NULL} (default) follows \code{font_size}.}

\item{forecast_size}{Optional single positive number giving the forecast
median line width. For \code{plot = "consistency"} only. \code{NULL} (default) uses
the built-in width.}

\item{forecast_color}{Optional single color (e.g. \code{"#1f77b4"} or
\code{"firebrick"}) applied to every forecast median line and its markers. For
\code{plot = "consistency"} only. Overrides the recency-based coloring (the
solid-newest / dashed-older distinction is kept); the prediction-interval
bands keep their styling. \code{NULL} (default) keeps the recency colors.}

\item{observed_size}{Optional single positive number giving the observed
(truth) line width. For \code{plot = "consistency"} only. \code{NULL} (default) uses
the built-in width.}

\item{observed_color}{Optional single color applied to the observed (truth)
line. For \code{plot = "consistency"} only. \code{NULL} (default) keeps the built-in
color.}

\item{aux_size}{Optional single positive number giving the auxiliary-variable
line width (secondary axis). For \code{plot = "consistency"} only. \code{NULL}
(default) uses the built-in width.}

\item{aux_color}{Optional color or vector of colors for the auxiliary-variable
lines (secondary axis). A single value colors every aux line; a vector acts
as a palette, cycled across the aux lines in order. For
\code{plot = "consistency"} only. \code{NULL} (default) keeps the built-in colors.}

\item{title_gap}{Optional single positive number giving the standoff in
pixels between the axis tick labels (the numbers) and the axis title --
useful when large fonts make them crowd. For \code{plot = "consistency"} only.
\code{NULL} (default) uses plotly's automatic spacing.}

\item{view_start, view_end}{Optional \code{Date}s (or \code{"YYYY-MM-DD"} strings)
giving the initial visible x-window. The data is unchanged -- only the
opening range differs -- so all of it remains pannable. Supply both or
neither; \code{NULL} (default) shows the full default window.}

\item{legend}{Logical. For a static image export, draw the side legend
panel? \code{TRUE} (default) reserves a right-hand column for it, matching the
report; \code{FALSE} hides it and lets the plot fill the full capture width.
HTML exports keep their interactive (collapsible) legend regardless.}

\item{overwrite}{Logical. Overwrite existing files? Defaults to \code{TRUE}.}

\item{plot_styles}{A named list of plot style settings produced by
\code{create_plot_styles()}. If \code{NULL} (default), package defaults are used.}

\item{eval_config}{A named list produced by \code{create_evaluation_config()}.
If \code{NULL} (default), package defaults are used.}

\item{quiet}{Logical. If \code{TRUE} (default), suppresses rendering console
output. Set to \code{FALSE} for verbose output useful when debugging.}
}
\value{
Invisibly returns the path(s) to the saved plot file(s).
}
\description{
Builds and saves the interactive forecast figure for a set of report inputs
without producing the full evaluation report. This is for users who want
only the forecast plot -- as an interactive HTML file, or as a static PNG,
JPEG, or PDF image -- rather than the complete document.
}
\details{
It takes the same completed options file as \code{generate_report()} and runs the
identical data-assembly pipeline, but stops as soon as the forecast plot has
been written: no master-data CSV is saved and none of the downstream report
sections are produced. A static export captures the same plot the report
shows -- floating side legend, full data range, and no range slider -- as an
image.

Static formats (PNG, JPEG, PDF) require \pkg{webshot2} (headless Chrome): the
image is a screenshot of the rendered plot, so it matches the report exactly.
HTML export needs no backend.
}
\examples{
\dontrun{
  # Interactive HTML of every geography, into a folder
  save_forecast_plot("report_options.R", file = "exports/")

  # One geography as a high-resolution PDF
  save_forecast_plot(
    "report_options.R",
    file     = "exports/upstate.pdf",
    location = "Upstate",
    width    = 1400,
    height   = 800
  )

  # All geographies as sharp JPEGs
  save_forecast_plot("report_options.R", file = "exports/",
                     format = "jpeg", scale = 2)
}

}
