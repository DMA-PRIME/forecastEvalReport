% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_evaluation_config.R
\name{create_evaluation_config}
\alias{create_evaluation_config}
\title{Create an evaluation configuration for forecast evaluation reports}
\usage{
create_evaluation_config(
  non_transmission_months = c(5, 6, 7),
  season_start_day_month = "August 01",
  peak_window = 20,
  stable_threshold = 10,
  pct_error_cushion = 20,
  timing_tol_steps = 0L,
  mag_tol = 0.8
)
}
\arguments{
\item{non_transmission_months}{Integer vector of calendar months (1-12)
treated as the off-season. These rows are kept for plotting but excluded
from every aggregate metric. Default \code{c(5, 6, 7)} (May-July).}

\item{season_start_day_month}{Character "Month DD" (e.g. \code{"August 01"})
marking the season start. Only the month is used, to assign each row to a
season for the peak-phase metrics. Default \code{"August 01"}.}

\item{peak_window}{Numeric. Tolerance around the observed seasonal max, as a
PERCENT of that max, used to define the contiguous peak phase. Default
\code{20}.}

\item{stable_threshold}{Numeric. Observed-count floor at/above which a row is
counted as "stable" for the forecast-bias percentage-error summaries.
Default \code{10}.}

\item{pct_error_cushion}{Numeric. Percentage-error band (in points) around
zero within which a forecast-bias row counts as "Within Range"; beyond
+/- this it is "Overestimate" / "Underestimate". Default \code{20}.}

\item{timing_tol_steps}{Numeric. Tolerance, in time steps, within which a
peak-phase timing miss still counts as "On Time". Default \code{1}.}

\item{mag_tol}{Numeric between 0 and 1. Minimum rank-matched agreement
at/above which a peak-phase magnitude counts as "On Target". Default
\code{0.80}.}
}
\value{
A named list of evaluation settings for use by
\code{build_testing_evaluation()}.
}
\description{
Builds a named list of the tuning settings used by the testing-period
evaluation metrics. All arguments have sensible defaults that match the
standard report behavior. Users who want to customize the evaluation create
their own config object by calling this function with overrides, then source
it in their options file and pass it to \code{\link[=generate_report]{generate_report()}}. The settings
flow through to \code{build_testing_evaluation()} and on to the individual metric
helpers (percent agreement, forecast bias, peak phase).
}
